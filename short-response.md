1. it is important for each collaborator to have made a branch with their name on it because then they could work without disrupting the main branch. 
2. developers use branches to work or fix work without editing the main branch.
3. git is a distributed version control system that takes a snapshot of the code in the moment. github is a cloud-based service that allows developers to store, track, manage, and control changes to their code. git and github work together so when developers are working on github, they are able to snapshot and manage their code with git. 
4. the command sequence is in the wrong order, the correct code is:
```
git add .
git commit -m "saving work"
git push
```